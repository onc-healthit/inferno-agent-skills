interface:
  display_name: "FHIR IG Change Ledger"
  short_description: "Directly compare FHIR IG versions and build IG-only raw ledgers."
  default_prompt: "Use this skill to directly compare two FHIR IG versions, create a reviewable narrative diff, and produce an IG-only raw change ledger."
